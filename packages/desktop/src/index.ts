/**
 * @zenvu/desktop
 * Tauri/Electron adapters for compiling Zenvu.js applications to macOS, Windows, and Linux binaries.
 */
export function createDesktopWindow(options: any) {
  console.log("Desktop Window initialized with options:", options);
}
